<h1>タスク新規作成</h1>

<%= form_with(model: @task,local: true) do |f| %>
  <%= f.label :content,"内容" %>
  <%= f.text_field :content %>
  
  <%= f.submit "投稿" %>
<% end %>

<p><%= link_to "タスク一覧へ",tasks_path %></p>
